<IfModule mod_rewrite.c>
    RewriteEngine On

    # Automatically route all domain traffic into the public/ directory on Hostinger
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
